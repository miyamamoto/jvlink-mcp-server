# JVLink MCP Server Configuration

# Database Type: sqlite (default), duckdb, postgresql
DB_TYPE=sqlite

# Database Path (for SQLite and DuckDB)
# SQLite: 標準的、設定不要、単一ファイル
# DuckDB: 分析クエリが高速（集計・GROUP BYが2-10倍高速）
# Example: C:/Users/<username>/JVData/race.db
DB_PATH=/path/to/race.db

# PostgreSQL Connection String (if using PostgreSQL)
# DB_CONNECTION_STRING=Host=localhost;Database=jvlink;Username=jvlink_user

# PostgreSQL Password (optional)
# JVLINK_DB_PASSWORD=your_password
