# JVLink MCP Server - Docker環境変数サンプル

# ============================================
# データベース設定
# ============================================

# データベースタイプ: sqlite (デフォルト), duckdb, postgresql
DB_TYPE=sqlite

# データベースパス（SQLite/DuckDB用）
# Dockerコンテナ内のパス: /data/race.db または /data/race.duckdb
DB_PATH=/data/race.db

# PostgreSQL接続設定（DB_TYPE=postgresql の場合のみ）
# DB_CONNECTION_STRING=Host=postgres;Database=jvlink;Username=jvlink_user
# JVLINK_DB_PASSWORD=jvlink_password

# ============================================
# MCPサーバー設定
# ============================================

# サーバーホスト
# - 0.0.0.0: すべてのIPアドレスからアクセス可能（リモート接続用）
# - 127.0.0.1: ローカルホストのみアクセス可能
MCP_HOST=0.0.0.0

# サーバーポート
MCP_PORT=8000

# ============================================
# Docker固有の設定
# ============================================

# ボリュームマウントのヒント（docker-compose.ymlで使用）
# ホスト側のデータディレクトリを /data にマウントしてください
# 例: ./data:/data:rw

# ============================================
# セキュリティ注意事項
# ============================================
# 本番環境では以下の対策を実施してください：
# 1. 認証機能の実装（API Key、Bearer Token）
# 2. HTTPS/TLS の使用
# 3. ファイアウォール設定
# 4. アクセス制御（IPアドレス制限）
